name: Propagate to Templates

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  propagate:
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.sync.outputs.changed }}
      matrix: ${{ steps.matrix.outputs.matrix }}
    steps:
      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.VIBESTACK_AGENT_APP_ID }}
          private-key: ${{ secrets.VIBESTACK_AGENT_PRIVATE_KEY }}
          owner: VibeStackCodes

      - name: Checkout base template
        uses: actions/checkout@v4
        with:
          path: base

      - name: Checkout templates repo
        uses: actions/checkout@v4
        with:
          repository: VibeStackCodes/vibestack-templates
          token: ${{ steps.app-token.outputs.token }}
          path: templates

      - name: Run propagation script
        id: sync
        run: bash base/.github/scripts/propagate.sh base templates

      - name: Generate matrix
        id: matrix
        if: steps.sync.outputs.changed == 'true'
        run: |
          cd templates
          DIRS=$(ls -d */ | sed 's/\///' | jq -R . | jq -sc '.')
          echo "matrix=$DIRS" >> "$GITHUB_OUTPUT"

      - name: Commit and push
        if: steps.sync.outputs.changed == 'true'
        run: |
          cd templates
          SHORT_SHA=$(cd ../base && git rev-parse --short HEAD)
          git config user.name "vibestack-agent[bot]"
          git config user.email "vibestack-agent[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore: sync base template ($SHORT_SHA)"
          git push

  validate:
    needs: propagate
    if: needs.propagate.outputs.changed == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        template: ${{ fromJson(needs.propagate.outputs.matrix) }}
    steps:
      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.VIBESTACK_AGENT_APP_ID }}
          private-key: ${{ secrets.VIBESTACK_AGENT_PRIVATE_KEY }}
          owner: VibeStackCodes

      - name: Checkout templates repo
        uses: actions/checkout@v4
        with:
          repository: VibeStackCodes/vibestack-templates
          token: ${{ steps.app-token.outputs.token }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Build template
        run: |
          cd ${{ matrix.template }}
          bun install --frozen-lockfile || bun install
          bun run build
